version: v1.1.0
steps:
# Build target image
- build: -t {{.Run.Registry}}/spring-app-jp:{{.Run.ID}} ./demo
# Run image 
- cmd: -t {{.Run.Registry}}/spring-app-jp:{{.Run.ID}}
  id: test
  detach: true
  ports: ["8080:80"]
- cmd: docker stop test
  id: stop-test
# Push image
- push:
  - {{.Run.Registry}}/spring-app-jp:{{.Run.ID}}